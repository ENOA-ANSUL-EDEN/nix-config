patch:
  #schema/name: 💎琉璃·四二
  embeded_cands/+:
    first_format: "${Stash}${Code}${候選}"
    next_format: ""
    

  ascii_composer/switch_key/+:
    # 左Shift上屏编码  右Shift上屏候选
    Shift_L: commit_code
    Shift_R: commit_text
    Control_L: noop
    Control_R: noop
    Caps_Lock: clear
    Eisu_toggle: clear